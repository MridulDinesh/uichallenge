import 'package:flutter/material.dart';

class Searching_bar extends StatelessWidget {
  const Searching_bar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
